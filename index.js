function divide(){
    const result = 2000 / 100;
    return 20;
}

const add= (a, b) => (a + b);
    let a = 3;
    let b = 4;
    const result = add(3, 4);
    console.log(result); // output: 7, because 3 + 4 is 7

    const square = (number) => number * number;
    console.log(square(0)); // output: 0, because 0 squared is 0

  
  





